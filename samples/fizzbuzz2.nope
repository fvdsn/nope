
# This is FizzBuzz 'nope' style bitchez


var i = 1, while i <= 100 (
    let divby3 = i % 3 == 0
    let divby5 = i % 5 == 0

    if divby5 && divby3 ( 
        print 'Fizzbuzz'
    ) else if divby3 (
        print 'Fizz'
    ) else if divby5 (
        print 'Buzz'
    ) else (
        print i
    )

    set i = i + 1
)

